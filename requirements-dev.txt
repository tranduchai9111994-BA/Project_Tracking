# Dev / test only — không cài khi chạy start.bat production
-r requirements.txt
pytest==8.3.4
pytest-cov==6.0.0
# T30 — mock HTTP cho unit test module Registry API (analyzer/integrations.py)
requests-mock==1.12.1
