# iHRP Function List Tracker — Python runtime dependencies.
# KHÔNG cài pandas/numpy: parser tự đọc Excel qua openpyxl để tiết kiệm dung
# lượng. Chart aggregate viết tay bằng Python vanilla + collections.

# --- Web framework ---
Flask==3.1.1
# Flask transitives (pin để đảm bảo repro):
Werkzeug==3.1.8
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.4.2
blinker==1.9.0
colorama==0.4.6

# --- Excel I/O (parser + exporter) ---
openpyxl==3.1.5
et_xmlfile==2.0.0

# --- Registry API integrations (T30) ---
# requests: HTTP client cho luồng login + tải file Excel từ ứng dụng nguồn.
# beautifulsoup4: parse CSRF token trong HTML form login (fallback regex vẫn
#   hoạt động khi bs4 không có, nhưng bs4 an toàn hơn với HTML lộn xộn).
requests==2.32.3
beautifulsoup4==4.12.3
soupsieve==2.6

# --- Type hints (Python 3.9+ compat) ---
typing_extensions==4.16.0

# --- Database integration drivers (T31 — OPTIONAL) ---
# Chỉ cần cài khi user dùng auth.method='database' cho integration.
# Backend lazy-import → không cài vẫn dùng được các HTTP method khác.
# Uncomment / cài thủ công driver phù hợp với DB đích:
#   pyodbc>=5.0.0            # SQL Server (Windows: cần ODBC Driver 17/18)
#   psycopg2-binary>=2.9.0   # PostgreSQL
#   pymysql>=1.1.0           # MySQL / MariaDB

# --- Dev / testing (optional cho user cuối) ---
pytest==8.3.4
pytest-cov==6.0.0
coverage==7.15.2
iniconfig==2.3.0
pluggy==1.6.0
packaging==26.2
